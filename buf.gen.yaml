version: v2
plugins:
  # protoc-gen-zararpc owns the gRPC server registration + client stubs
  # (.grpc.go) and the package bridge (grpc_bridge.go) — no protoc-gen-go-grpc.
  - local: protoc-gen-zararpc
    out: proto
    opt:
      - paths=source_relative